// The default counter-app test was removed along with the counter app.
// Engine math is covered in test/engine/. Widget tests come with their features.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
